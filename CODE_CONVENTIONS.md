# 코드 컨벤션

직접 작성하는 C++ 및 GLSL 코드는 줄바꿈을 충분히 사용하는 스타일로 통일한다.
자동 포맷 기준은 저장소 루트의 `.clang-format`이며 clang-format 19 이상을 사용한다.

- 들여쓰기는 공백 4칸을 사용한다.
- 함수, 클래스, 구조체, 조건문, 반복문의 여는 중괄호는 다음 줄에 둔다.
- 짧은 함수와 한 줄짜리 조건문·반복문도 여러 줄로 펼친다.
- 조건문과 반복문은 본문이 한 문장이어도 중괄호를 사용한다.
- 한 줄에 여러 실행문을 붙여 쓰지 않는다.
- 함수 정의 사이에는 빈 줄을 둔다.
- 서로 다른 처리 단계는 적절한 빈 줄로 구분한다.
- 긴 인자 목록은 줄바꿈하고, 줄바꿈된 목록은 인자별로 나눈다.
- 연산자와 쉼표 주변에 일관된 공백을 둔다.
- 포인터와 참조 기호는 타입에 붙인다: `Renderer*`, `const Mesh&`.
- include 순서를 유지한다. 특히 `stdafx.h`를 사용하는 소스는 이를 먼저 포함한다.
- 한국어 문자열을 포함한 소스 파일은 UTF-8로 저장한다.
- 외부 라이브러리인 `SimpleGame/Dependencies`와 자동 생성 파일은 포맷 대상에서 제외한다.

예시:

```cpp
void Update()
{
    if (!m_ready)
    {
        return;
    }

    for (int i = 0; i < count; ++i)
    {
        UpdateItem(i);
    }
}
```

포맷은 기능 수정과 구분한다. 빌드와 실행 결과 확인은 사용자가 명시적으로 요청한 경우에만 수행한다.
